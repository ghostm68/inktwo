# 1. Allow everyone to access the logger script
# (Necessary so visitors' browsers can send data)
<Files "index.php">
    Require all granted
</Files>

# 2. Block everyone from downloading your data
# (The PHP script can still write to it, but the public cannot read it)
<Files "hits.log">
    Require all denied
</Files>

# 3. Prevent people from seeing a list of files in this folder
Options -Indexes
