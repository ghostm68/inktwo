[build]
  # Command to run the build process
  command = "npm run build" # Or your specific build command
  # Directory where the build output is placed
  publish = "dist" # Or your specific publish directory (e.g., "build")
