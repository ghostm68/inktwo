# Allow every bot to crawl everything
User-agent: *
Allow: /

# Block only the legacy cgi-bin directory
Disallow: /cgi-bin/

# Point Google (and all others) to the XML sitemap
Sitemap: https://inkrealm.info/sitemap.xml
